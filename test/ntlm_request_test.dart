import 'package:ntlm_request/ntlm_request.dart';
import 'package:test/test.dart';

void main() {
  // to test this package, you need to insert it into a api server
  // and call the route you've created using Google Chrome or Edge
  // you can use PostMan too, but remember to set autorization to ntlm
}
